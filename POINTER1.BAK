//program to illustrate pointer

#include<stdio.h>
#include<conio.h>

void main()
{
   int a,b,*p1,*p2;
   clrscr();
   printf("\n\tenter 2 integers");
   scanf("%d%d",&a,&b);
   p1=&a;//initialisation of pointer
   p2=&b;
   printf("\n\tvalue of a is=%d",*p1);
   printf("\n\tvalue of b is=%d",*p2);
   printf("\n\taddress of a is=%u",p1);//displays address in decimal format
 printf("\n\taddress of b is=%p",p2);//displays address in hexadecimal format
printf("\n\taddress of b is=%x",p2);//displays address in hexadecimal format
   getch();
}